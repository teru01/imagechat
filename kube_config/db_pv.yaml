apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
  labels:
    env: prod
    type: local
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/mysql
